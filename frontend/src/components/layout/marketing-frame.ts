/** Side gutters on md+ — matches hero and marketing sections. */
export const MARKETING_GUTTER_WIDTH = "80px";

/** Horizontal padding on the main content column (inside gutters). */
export const marketingContentPaddingX = { base: 6, md: 10 } as const;
